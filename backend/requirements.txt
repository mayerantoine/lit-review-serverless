# ============================================================================
# Local development dependencies
# Each Lambda / agent has its own requirements.txt for deployment.
# ============================================================================

# AWS
boto3>=1.42.55

# OpenAI
openai>=2.22.0
openai-agents>=0.8.4

# LangChain (used by shared/vectorstore.py and shared/pipeline.py)
langchain-core>=0.3.0
langchain-openai>=0.1.0
langchain-text-splitters>=0.0.1

# Data processing
numpy>=2.0.2
pandas>=2.3.3
pydantic>=2.0.0
tqdm>=4.66.0

# Multipart parsing (Lambda 1)
python-multipart>=0.0.9

# AgentCore SDK (agents)
bedrock-agentcore>=1.6.0

# Testing
moto[dynamodb,s3]>=5.0.0
pytest>=8.0.0
