# ============================================================================
# Lambda Dependencies - Optimized for AWS Lambda deployment
# ============================================================================
# This file contains only the minimal dependencies needed for production.
# Unused packages (langchain, langchain-community) have been removed.
# Total package size target: < 65MB zipped
# ============================================================================

# Core AWS and API Framework
boto3>=1.42.55
fastapi>=0.128.8
mangum>=0.19.0
uvicorn>=0.39.0

# OpenAI and Agents
openai>=2.22.0
openai-agents>=0.8.4

# LangChain - Minimal (only what vectorstore.py uses)
langchain-core>=0.3.0
langchain-openai>=0.1.0
langchain-text-splitters>=0.0.1

# Data Processing
numpy>=2.0.2
pandas>=2.3.3
pydantic>=2.0.0

# Utilities
python-dotenv>=1.0.0
tqdm>=4.66.0
